import { NavLink } from 'react-router-dom';
import useFetch from '../hooks/useFetch';
function Stat({ label, value, tone = '' }) { return <div className="stat-card"><span>{label}</span><strong className={tone}>{value}</strong></div>; }
export default function Overview() { const { data } = useFetch('/reports/summary', {}); return <><div className="page-title"><div><span className="eyebrow">NETWORK OVERVIEW</span><h1>Today at a glance</h1></div><span className="live-pill"><i /> Live data</span></div><div className="stats"><Stat label="Pending maintenance" value={data.pendingTasks ?? '—'} tone="orange" /><Stat label="Approved blocks" value={data.approvedPlans ?? '—'} tone="blue" /><Stat label="Critical defects" value={data.criticalTasks ?? '—'} tone="red" /><Stat label="Total tasks" value={data.totalTasks ?? '—'} /></div><div className="panel welcome"><div><span className="eyebrow">AI-COORDINATED PLANNING</span><h2>Plan safer blocks with confidence.</h2><p>Review priorities, coordinate departments, and monitor corridor availability from one operational view.</p></div><NavLink className="primary" to="/calendar">Open block calendar</NavLink></div></>; }
